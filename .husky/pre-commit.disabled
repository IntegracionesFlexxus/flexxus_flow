#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit hook - Ejecuta lint-staged en ambos proyectos

echo '🔍 Ejecutando lint-staged en frontend...'
cd frontend-v1 && npx lint-staged
cd ..

echo '🔍 Ejecutando lint-staged en backend...'
cd backend && npx lint-staged
cd ..